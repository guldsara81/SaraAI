[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "SaraAI"
version = "0.1.0"
description = "Open Source Artificial Intelligence"
requires-python = ">=3.12"

authors = [
    {name = "Sara Johansson"}
]
